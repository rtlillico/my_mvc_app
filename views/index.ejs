<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= pageTitle %></title>
</head>
<body>
    <h1>Welcome to the MVC App</h1>
    <ul>
        <% users.forEach(user => { %>
            <li><%= user.name %> (<%= user.email %>)</li>
        <% }); %>
    </ul>
</body>
</html>
